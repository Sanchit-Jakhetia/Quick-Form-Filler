{
  "name": "quick-form-filler-extension",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc -p tsconfig.json && node scripts/build.mjs",
    "dev": "tsc -p tsconfig.json --watch --noEmit",
    "test": "node --test dist/tests/search.test.js"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "@types/node": "^22.7.5",
    "esbuild": "^0.28.2",
    "typescript": "^5.6.3"
  }
}
